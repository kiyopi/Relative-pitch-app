# プレミアム分析ページ 設計仕様書

**作成日**: 2025-11-19
**最終更新**: 2025-11-19
**バージョン**: 2.1.0
**対象**: `/PitchPro-SPA/pages/premium-analysis.html`
**目的**: 拡張性を重視した詳細分析ページの包括的設計

## 📝 変更履歴

### v2.1.0（2025-11-19）
- **データ構造修正**: セッションデータの構造を`steps`配列から`pitchErrors`配列に統一
- **MODE_DEFINITIONS更新**: 絵文字（`level`）をLucideアイコン（`levelIcon`）に変更
- **色の統一**: トップページと詳細分析のモード色を統一（beginner: green, intermediate: orange）
- **アイコン統一**: トップページと同じLucideアイコンを使用（shuffle, zap, music, locate-fixed）

---

## 📋 目次

1. [概要](#1-概要)
2. [モード構成](#2-モード構成)
3. [全体設計方針](#3-全体設計方針)
4. [タブ構成](#4-タブ構成)
5. [モード別分析の詳細設計](#5-モード別分析の詳細設計)
6. [データ構造設計](#6-データ構造設計)
7. [UI設計（画面構成図）](#7-ui設計画面構成図)
8. [CSS設計](#8-css設計)
9. [⚠️ 実装上の重要な注意点](#9-️-実装上の重要な注意点)
10. [実装タスク](#10-実装タスク)
11. [将来の拡張計画](#11-将来の拡張計画)

---

## 1. 概要

### 1.1 目的
相対音感トレーニングの詳細分析を提供し、ユーザーの音感向上を科学的にサポートする。

### 1.2 基本方針
1. **拡張性最優先**: 将来のモード追加に容易に対応
2. **タブ数維持**: 4タブを維持（増やさない）
3. **データ完全性**: 全10モード（将来12+モード）を分析対象
4. **UI簡潔性**: 初期表示はシンプル、詳細は折りたたみ

### 1.3 ユーザー価値
- ✅ 音程間隔別の精度分析（相対音感の本質）
- ✅ 成長の可視化（モチベーション維持）
- ✅ 具体的な練習プラン（改善方法の提示）
- ✅ モード別の詳細統計（弱点の特定）
- ✅ 実験的な音階グループ分析（経験則の検証）

---

## 2. モード構成

### 2.1 現在の10モード

#### **初級: ランダム基音（2モード）**
- `random-ascending` - ランダム基音（上行音程）
- `random-descending` - ランダム基音（下行音程）

**特徴**:
- 8セッション
- カラオケで音程を合わせて歌える基礎トレーニング
- 基本的な相対音感の習得

#### **中級: 連続チャレンジ（2モード）**
- `continuous-ascending` - 連続チャレンジ（上行音程）
- `continuous-descending` - 連続チャレンジ（下行音程）

**特徴**:
- 12セッション連続
- クロマチック12音の聞き分け
- 集中力を維持しながら相対音感を鍛える

#### **上級: 12音階（6モード）**

**提示順序と音程方向の組み合わせ**:

| 提示順序 | 音程方向 | モードキー | 説明 |
|---------|---------|-----------|------|
| 上昇順（C→C#→D...） | 上行 | `twelve-asc-ascending` | 12音を上昇順で提示、基音から上行音程 |
| 上昇順（C→C#→D...） | 下行 | `twelve-asc-descending` | 12音を上昇順で提示、基音から下行音程 |
| 下降順（B→B♭→A...） | 上行 | `twelve-desc-ascending` | 12音を下降順で提示、基音から上行音程 |
| 下降順（B→B♭→A...） | 下行 | `twelve-desc-descending` | 12音を下降順で提示、基音から下行音程 |
| 両方向24回 | 上行 | `twelve-both-ascending` | 両方向24回、基音から上行音程 |
| 両方向24回 | 下行 | `twelve-both-descending` | 両方向24回、基音から下行音程 |

**特徴**:
- 12-24セッション
- すべての音に対して完璧な相対音感を習得
- プロレベル判定・S級認定

---

### 2.2 将来の拡張（Phase 5以降）

#### **特別: 弱点練習（2+モード）**
- `weakness-ascending` - 弱点練習（上行音程）
- `weakness-descending` - 弱点練習（下行音程）

**特徴**:
- AI分析による個別最適化
- あなたの弱点音程を自動分析
- 効率的に苦手を克服する練習プログラム
- カスタム練習・集中改善

**将来的にさらに増える可能性**:
- 音程特化練習（例: 3度特化、7度特化）
- リズム統合練習
- 和音認識練習
- etc.

---

## 3. 全体設計方針

### 3.1 設計課題

#### **課題1: 階層の複雑性**
12音階モードは**3階層構造**:
```
12音階（親モード）
├─ 上昇順（提示順序）
│  ├─ 上行（音程方向）
│  └─ 下行（音程方向）
├─ 下降順（提示順序）
│  ├─ 上行（音程方向）
│  └─ 下行（音程方向）
└─ 両方向（提示順序）
   ├─ 上行（音程方向）
   └─ 下行（音程方向）
```

#### **課題2: 拡張性**
- 現在10モード → 将来12+モード
- 親モードが増える可能性（現在3つ → 将来4+）

### 3.2 解決策: フラット＋グループ化アプローチ

**基本方針**:
1. **データ構造はフラット** - 全モードを平等に扱う
2. **UI表示はグループ化** - 視覚的に親モードで整理
3. **拡張性最優先** - 新モード追加が容易

**実装アプローチ**:
- 親モードカード（初期表示、折りたたみ）
- 詳細展開セクション（クリックで展開）
- 音程方向別比較グラフ
- サブグループ表示（12音階のみ）

---

## 4. タブ構成

### **Tab 1: 音程精度分析** ⭐⭐⭐⭐⭐
- ✅ 平均音程精度
- ✅ 音程間隔別精度（2度〜8度）
- ✅ 音階グループ別精度（実験的）
- ✅ 上行・下行比較（Phase 4で有効化）

**相対音感の本質**: 音程間隔の認識を完璧に捉えている

---

### **Tab 2: エラーパターン分析** ⭐⭐⭐⭐
- ✅ シャープ・フラット傾向
- ✅ 音程拡大・縮小パターン

**教育的価値**: 個人の癖を意識化し、具体的な改善策を提示

---

### **Tab 3: 練習プラン** ⭐⭐⭐⭐
- ✅ 優先度別練習プラン（緊急・重要・推奨）
- ✅ 具体的な練習方法

**実用性**: 次に何をすべきか明確、初心者にも実践しやすい

---

### **Tab 4: 成長記録** ⭐⭐⭐⭐⭐
- ✅ 月間成長比較
- ✅ 音程間隔別成長グラフ（Chart.js）
- ✅ 時系列パフォーマンス分析
- ✅ **モード別熟練度分析**（本仕様書の焦点）

**モチベーション**: 長期的な成長の可視化で継続学習を促進

---

## 5. モード別分析の詳細設計

### 5.1 初期表示（折りたたみ状態）

**親モードカード4つのみ表示**:

```
┌─────────────────────────────────────────────┐
│ 🎯 モード別熟練度分析                        │
│                                             │
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃ 🔰 初級: ランダム基音 (2モード)       ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │ 総セッション: 128 | 平均: ±28¢   │ ┃  │
│ ┃ │ 総合レベル: Lv.7 | 熟練度: 70%   │ ┃  │
│ ┃ │ ▓▓▓▓▓▓▓░░░                      │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┃              [詳細を見る ▼]            ┃  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
│                                             │
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃ 🥉 中級: 連続チャレンジ (2モード)     ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │ 総セッション: 64 | 平均: ±32¢    │ ┃  │
│ ┃ │ 総合レベル: Lv.5 | 熟練度: 50%   │ ┃  │
│ ┃ │ ▓▓▓▓▓░░░░░                      │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┃              [詳細を見る ▼]            ┃  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
│                                             │
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃ 🥇 上級: 12音階 (6モード)            ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │ 総セッション: 48 | 平均: ±38¢    │ ┃  │
│ ┃ │ 総合レベル: Lv.3 | 熟練度: 30%   │ ┃  │
│ ┃ │ ▓▓▓░░░░░░░                      │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┃              [詳細を見る ▼]            ┃  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
│                                             │
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃ 🎯 特別: 弱点練習 (2モード)           ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │ まだデータがありません              │ ┃  │
│ ┃ │ トレーニングを始めましょう          │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┃              [詳細を見る ▼]            ┃  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
└─────────────────────────────────────────────┘
```

**特徴**:
- シンプルで見やすい（4つのカードのみ）
- 各親モードの概要を一目で把握
- 興味のある親モードのみ展開可能

---

### 5.2 展開状態（初級・中級の場合）

**音程方向別の2カラム表示**:

```
┌─────────────────────────────────────────────┐
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃ 🔰 初級: ランダム基音 (2モード)       ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │ 総セッション: 128 | 平均: ±28¢   │ ┃  │
│ ┃ │ 総合レベル: Lv.7 | 熟練度: 70%   │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┃              [詳細を閉じる ▲]          ┃  │
│ ┃ ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄ ┃  │
│ ┃                                       ┃  │
│ ┃ 📊 音程方向別比較                      ┃  │
│ ┃                                       ┃  │
│ ┃ ┌────────────┐  ┌────────────┐       ┃  │
│ ┃ │ ↑ 上行音程  │  │ ↓ 下行音程  │       ┃  │
│ ┃ │            │  │            │       ┃  │
│ ┃ │ 68セッション│  │ 60セッション│       ┃  │
│ ┃ │   ±25¢    │  │   ±32¢    │       ┃  │
│ ┃ │   Lv.8     │  │   Lv.6     │       ┃  │
│ ┃ │ 熟練度 80% │  │ 熟練度 60% │       ┃  │
│ ┃ │ ▓▓▓▓▓▓▓▓░░ │  │ ▓▓▓▓▓▓░░░░ │       ┃  │
│ ┃ │            │  │            │       ┃  │
│ ┃ │ 📋 音程別  │  │ 📋 音程別  │       ┃  │
│ ┃ │ 2度: ±18¢ │  │ 2度: ±24¢ │       ┃  │
│ ┃ │ 3度: ±22¢ │  │ 3度: ±28¢ │       ┃  │
│ ┃ │ 4度: ±14¢ │  │ 4度: ±19¢ │       ┃  │
│ ┃ │ 5度: ±20¢ │  │ 5度: ±26¢ │       ┃  │
│ ┃ │ 6度: ±24¢ │  │ 6度: ±30¢ │       ┃  │
│ ┃ │ 7度: ±28¢ │  │ 7度: ±34¢ │       ┃  │
│ ┃ │ 8度: ±16¢ │  │ 8度: ±22¢ │       ┃  │
│ ┃ └────────────┘  └────────────┘       ┃  │
│ ┃                                       ┃  │
│ ┃ 📈 成長トレンド（最近30日）             ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │  Chart.js折れ線グラフ              │ ┃  │
│ ┃ │  上行: ±30¢ → ±25¢ (改善-5¢)      │ ┃  │
│ ┃ │  下行: ±35¢ → ±32¢ (改善-3¢)      │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
└─────────────────────────────────────────────┘
```

**特徴**:
- 上行・下行の2カラムで比較しやすい
- 各方向の詳細統計（レベル、熟練度、音程別精度）
- 成長トレンドグラフで改善度を可視化

---

### 5.3 展開状態（12音階の場合）

**提示順序別 → 音程方向別の3階層表示**:

```
┌─────────────────────────────────────────────┐
│ ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│ ┃ 🥇 上級: 12音階 (6モード)            ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │ 総セッション: 48 | 平均: ±38¢    │ ┃  │
│ ┃ │ 総合レベル: Lv.3 | 熟練度: 30%   │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┃              [詳細を閉じる ▲]          ┃  │
│ ┃ ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄ ┃  │
│ ┃                                       ┃  │
│ ┃ 📚 提示順序別分析                      ┃  │
│ ┃                                       ┃  │
│ ┃ ┌─ 上昇順（C→C#→D...）──────────┐  ┃  │
│ ┃ │                                  │  ┃  │
│ ┃ │  ┌──────────┐  ┌──────────┐  │  ┃  │
│ ┃ │  │↑ 上行音程 │  │↓ 下行音程 │  │  ┃  │
│ ┃ │  │ 8セッション│  │ 7セッション│  │  ┃  │
│ ┃ │  │  ±35¢   │  │  ±42¢   │  │  ┃  │
│ ┃ │  │  Lv.4    │  │  Lv.3    │  │  ┃  │
│ ┃ │  │ 熟練度40%│  │ 熟練度30%│  │  ┃  │
│ ┃ │  └──────────┘  └──────────┘  │  ┃  │
│ ┃ └──────────────────────────────┘  ┃  │
│ ┃                                       ┃  │
│ ┃ ┌─ 下降順（B→B♭→A...）──────────┐  ┃  │
│ ┃ │                                  │  ┃  │
│ ┃ │  ┌──────────┐  ┌──────────┐  │  ┃  │
│ ┃ │  │↑ 上行音程 │  │↓ 下行音程 │  │  ┃  │
│ ┃ │  │ 6セッション│  │ 5セッション│  │  ┃  │
│ ┃ │  │  ±38¢   │  │  ±45¢   │  │  ┃  │
│ ┃ │  │  Lv.3    │  │  Lv.2    │  │  ┃  │
│ ┃ │  │ 熟練度30%│  │ 熟練度20%│  │  ┃  │
│ ┃ │  └──────────┘  └──────────┘  │  ┃  │
│ ┃ └──────────────────────────────┘  ┃  │
│ ┃                                       ┃  │
│ ┃ ┌─ 両方向24回 ──────────────────┐  ┃  │
│ ┃ │                                  │  ┃  │
│ ┃ │  ┌──────────┐  ┌──────────┐  │  ┃  │
│ ┃ │  │↑ 上行音程 │  │↓ 下行音程 │  │  ┃  │
│ ┃ │  │12セッション│  │10セッション│  │  ┃  │
│ ┃ │  │  ±36¢   │  │  ±40¢   │  │  ┃  │
│ ┃ │  │  Lv.4    │  │  Lv.3    │  │  ┃  │
│ ┃ │  │ 熟練度40%│  │ 熟練度30%│  │  ┃  │
│ ┃ │  └──────────┘  └──────────┘  │  ┃  │
│ ┃ └──────────────────────────────┘  ┃  │
│ ┃                                       ┃  │
│ ┃ 📊 提示順序別比較グラフ                ┃  │
│ ┃ ┌─────────────────────────────────┐ ┃  │
│ ┃ │  Chart.js レーダーチャート          │ ┃  │
│ ┃ │  上昇順・下降順・両方向の比較        │ ┃  │
│ ┃ └─────────────────────────────────┘ ┃  │
│ ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
└─────────────────────────────────────────────┘
```

**特徴**:
- 提示順序（上昇順・下降順・両方向）でグループ化
- 各提示順序内で上行・下行を比較
- 3階層構造を視覚的に整理

---

## 6. データ構造設計

### 6.1 MODE_DEFINITIONS（JavaScript）

```javascript
// premium-analysis-calculator.js
const MODE_DEFINITIONS = {
  // 親モードグループ
  parentModes: {
    'beginner': {
      name: '初級: ランダム基音',
      displayName: 'ランダム基音',
      icon: 'shuffle',
      color: 'green',
      levelIcon: 'shuffle', // Lucideアイコン名（絵文字から移行）
      modes: ['random-ascending', 'random-descending']
    },
    'intermediate': {
      name: '中級: 連続チャレンジ',
      displayName: '連続チャレンジ',
      icon: 'zap',
      color: 'orange',
      levelIcon: 'zap', // Lucideアイコン名（絵文字から移行）
      modes: ['continuous-ascending', 'continuous-descending']
    },
    'advanced': {
      name: '上級: 12音階',
      displayName: '12音階',
      icon: 'music',
      color: 'purple',
      levelIcon: 'music', // Lucideアイコン名（絵文字から移行）
      modes: [
        'twelve-asc-ascending',
        'twelve-asc-descending',
        'twelve-desc-ascending',
        'twelve-desc-descending',
        'twelve-both-ascending',
        'twelve-both-descending'
      ],
      // サブグループ定義（12音階のみ）
      subgroups: {
        'ascending-order': {
          name: '上昇順（C→C#→D...）',
          modes: ['twelve-asc-ascending', 'twelve-asc-descending']
        },
        'descending-order': {
          name: '下降順（B→B♭→A...）',
          modes: ['twelve-desc-ascending', 'twelve-desc-descending']
        },
        'both-directions': {
          name: '両方向24回',
          modes: ['twelve-both-ascending', 'twelve-both-descending']
        }
      }
    },
    'weakness': { // 将来の拡張
      name: '特別: 弱点練習',
      displayName: '弱点練習',
      icon: 'locate-fixed',
      color: 'orange',
      levelIcon: 'locate-fixed', // Lucideアイコン名（絵文字から移行）
      modes: ['weakness-ascending', 'weakness-descending']
    }
  },

  // 個別モード定義
  modes: {
    // ランダム基音
    'random-ascending': {
      name: 'ランダム基音（上行）',
      displayName: '上行音程',
      parent: 'beginner',
      direction: 'ascending',
      icon: 'arrow-up'
    },
    'random-descending': {
      name: 'ランダム基音（下行）',
      displayName: '下行音程',
      parent: 'beginner',
      direction: 'descending',
      icon: 'arrow-down'
    },

    // 連続チャレンジ
    'continuous-ascending': {
      name: '連続チャレンジ（上行）',
      displayName: '上行音程',
      parent: 'intermediate',
      direction: 'ascending',
      icon: 'arrow-up'
    },
    'continuous-descending': {
      name: '連続チャレンジ（下行）',
      displayName: '下行音程',
      parent: 'intermediate',
      direction: 'descending',
      icon: 'arrow-down'
    },

    // 12音階 - 上昇順
    'twelve-asc-ascending': {
      name: '12音階 上昇順（上行）',
      displayName: '上行音程',
      parent: 'advanced',
      subgroup: 'ascending-order',
      direction: 'ascending',
      icon: 'arrow-up'
    },
    'twelve-asc-descending': {
      name: '12音階 上昇順（下行）',
      displayName: '下行音程',
      parent: 'advanced',
      subgroup: 'ascending-order',
      direction: 'descending',
      icon: 'arrow-down'
    },

    // 12音階 - 下降順
    'twelve-desc-ascending': {
      name: '12音階 下降順（上行）',
      displayName: '上行音程',
      parent: 'advanced',
      subgroup: 'descending-order',
      direction: 'ascending',
      icon: 'arrow-up'
    },
    'twelve-desc-descending': {
      name: '12音階 下降順（下行）',
      displayName: '下行音程',
      parent: 'advanced',
      subgroup: 'descending-order',
      direction: 'descending',
      icon: 'arrow-down'
    },

    // 12音階 - 両方向
    'twelve-both-ascending': {
      name: '12音階 両方向（上行）',
      displayName: '上行音程',
      parent: 'advanced',
      subgroup: 'both-directions',
      direction: 'ascending',
      icon: 'arrow-up'
    },
    'twelve-both-descending': {
      name: '12音階 両方向（下行）',
      displayName: '下行音程',
      parent: 'advanced',
      subgroup: 'both-directions',
      direction: 'descending',
      icon: 'arrow-down'
    },

    // 弱点練習（将来）
    'weakness-ascending': {
      name: '弱点練習（上行）',
      displayName: '上行音程',
      parent: 'weakness',
      direction: 'ascending',
      icon: 'arrow-up'
    },
    'weakness-descending': {
      name: '弱点練習（下行）',
      displayName: '下行音程',
      parent: 'weakness',
      direction: 'descending',
      icon: 'arrow-down'
    }
  }
};
```

### 6.2 セッションデータ構造

**重要**: セッションデータは`pitchErrors`配列を使用（`steps`配列ではない）

```javascript
// localStorageに保存されるセッションデータの構造
const sessionData = {
  sessionId: 1,
  lessonId: 'lesson_123',
  mode: 'random',
  chromaticDirection: 'random', // 基音進行方向
  scaleDirection: 'ascending',  // 音階方向
  baseNote: 'C4',
  baseFrequency: 261.63,
  startTime: 1700000000000,
  endTime: 1700000060000,
  duration: 60000,
  completed: true,

  // ★ 重要: pitchErrors配列を使用（steps配列ではない）
  pitchErrors: [
    {
      step: 0,                    // ステップ番号（0-7: ド-ド）
      expectedNote: 'C4',         // 期待される音名
      expectedFrequency: 261.63,  // 期待される周波数（Hz）
      detectedFrequency: 262.0,   // 検出された周波数（Hz）
      errorInCents: 2.5,          // セント単位の誤差（±200以内）
      clarity: 0.85,              // 明瞭度（0-1）
      volume: 0.65,               // 音量（0-1）
      timestamp: 1700000001000    // タイムスタンプ
    },
    // ... 8ステップ分
  ]
};
```

**データアクセスパターン**:
```javascript
// ✅ 正しい方法
sessionData.forEach(session => {
  if (!session.pitchErrors || !Array.isArray(session.pitchErrors)) return;

  session.pitchErrors.forEach(pitchErrorData => {
    const absError = Math.abs(pitchErrorData.errorInCents);
    const interval = pitchErrorData.step + 1; // step番号から音程を計算
    const note = pitchErrorData.expectedNote;
  });
});

// ❌ 間違った方法（古い実装）
// session.steps は存在しない
// step.pitchError は存在しない
```

### 6.3 セッションデータの正規化

```javascript
/**
 * セッションデータからモードキーを正規化
 */
function normalizeSessionMode(session) {
  const mode = session.mode || 'random';
  const direction = session.direction || 'ascending';
  const order = session.order; // 'asc', 'desc', 'both' (12音階のみ)

  // 12音階の場合
  if (mode === 'twelve' || mode === '12tone') {
    const orderKey = order || 'asc';
    return `twelve-${orderKey}-${direction}`;
  }

  // それ以外のモード
  return `${mode}-${direction}`;
}
```

### 6.4 親モード集計

```javascript
/**
 * 親モード別の統計を計算
 */
function calculateParentModeStats(sessionData, parentModeKey) {
  const parentMode = MODE_DEFINITIONS.parentModes[parentModeKey];
  const childModes = parentMode.modes;

  // 各子モードのセッションをフィルタリング
  const parentSessions = sessionData.filter(session => {
    const modeKey = normalizeSessionMode(session);
    return childModes.includes(modeKey);
  });

  // 統計計算
  const avgError = calculateAverageError(parentSessions);
  const totalSessions = parentSessions.length;
  const level = calculateMasteryLevel(parentModeKey, parentSessions);
  const masteryRate = calculateMasteryRate(level);

  return {
    name: parentMode.name,
    displayName: parentMode.displayName,
    icon: parentMode.icon,
    color: parentMode.color,
    level: parentMode.level,
    totalSessions,
    avgError,
    masteryLevel: level,
    masteryRate,
    childModes: calculateChildModeStats(childModes, sessionData)
  };
}
```

---

## 7. UI設計（画面構成図）

### 7.1 モバイル表示（スマホ画面）

```
┌─────────────────┐
│ 📊 詳細分析      │
│ [音程][エラー]  │
│ [練習][成長]    │
├─────────────────┤
│ 🎯 モード別分析  │
│                 │
│ ┏━━━━━━━━━━━━┓ │
│ ┃🔰ランダム基音┃ │
│ ┃             ┃ │
│ ┃ 128セッション┃ │
│ ┃   ±28¢     ┃ │
│ ┃   Lv.7     ┃ │
│ ┃ 熟練度 70%  ┃ │
│ ┃ ▓▓▓▓▓▓▓░░░ ┃ │
│ ┃             ┃ │
│ ┃ [詳細 ▼]    ┃ │
│ ┗━━━━━━━━━━━━┛ │
│                 │
│ ┏━━━━━━━━━━━━┓ │
│ ┃🥉連続チャレンジ┃ │
│ ┃（同様）      ┃ │
│ ┗━━━━━━━━━━━━┛ │
│                 │
│ ┏━━━━━━━━━━━━┓ │
│ ┃🥇 12音階     ┃ │
│ ┃（同様）      ┃ │
│ ┗━━━━━━━━━━━━┛ │
│                 │
│ ┏━━━━━━━━━━━━┓ │
│ ┃🎯 弱点練習   ┃ │
│ ┃（同様）      ┃ │
│ ┗━━━━━━━━━━━━┛ │
└─────────────────┘
```

### 7.2 タブレット・PC表示

**親モードカード**:
- タブレット: 2カラムグリッド
- PC: 2カラムグリッド（幅広）

**詳細展開時の音程方向カード**:
- タブレット: 1カラム縦並び
- PC: 2カラムグリッド

---

## 8. CSS設計

### 8.1 親モードカード

```css
/* 親モードカード */
.parent-mode-card {
  background: linear-gradient(135deg,
    var(--mode-color-light),
    var(--mode-color-dark));
  border: 2px solid var(--mode-color-border);
  border-radius: 16px;
  padding: 1.5rem;
  margin-bottom: 1rem;
  transition: all 0.3s ease;
}

.parent-mode-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
}

/* 親モード別カラー */
.parent-mode-card[data-level="beginner"] {
  --mode-color-light: rgba(59, 130, 246, 0.1);
  --mode-color-dark: rgba(37, 99, 235, 0.05);
  --mode-color-border: rgba(59, 130, 246, 0.3);
}

.parent-mode-card[data-level="intermediate"] {
  --mode-color-light: rgba(16, 185, 129, 0.1);
  --mode-color-dark: rgba(5, 150, 105, 0.05);
  --mode-color-border: rgba(16, 185, 129, 0.3);
}

.parent-mode-card[data-level="advanced"] {
  --mode-color-light: rgba(168, 85, 247, 0.1);
  --mode-color-dark: rgba(126, 34, 206, 0.05);
  --mode-color-border: rgba(168, 85, 247, 0.3);
}

.parent-mode-card[data-level="weakness"] {
  --mode-color-light: rgba(245, 158, 11, 0.1);
  --mode-color-dark: rgba(217, 119, 6, 0.05);
  --mode-color-border: rgba(245, 158, 11, 0.3);
}

/* 親モードヘッダー */
.parent-mode-header {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-bottom: 1rem;
}

.parent-mode-level {
  font-size: 1.5rem;
}

.parent-mode-title {
  color: white;
  font-size: 1.125rem;
  font-weight: 600;
  flex: 1;
}

.parent-mode-count {
  color: #94a3b8;
  font-size: 0.875rem;
}

/* 統計グリッド */
.mode-summary-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 1rem;
}

.stat-item {
  text-align: center;
  padding: 0.75rem;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 8px;
}

.stat-label {
  color: #94a3b8;
  font-size: 0.75rem;
  display: block;
  margin-bottom: 0.25rem;
}

.stat-value {
  color: white;
  font-size: 1.25rem;
  font-weight: 700;
}

/* 詳細ボタン */
.mode-detail-btn {
  width: 100%;
  padding: 0.75rem;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  color: #cbd5e1;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

.mode-detail-btn:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: rgba(255, 255, 255, 0.2);
}
```

### 8.2 詳細展開セクション

```css
/* 詳細セクション */
.mode-detail-section {
  margin-top: 1rem;
  padding: 1.5rem;
  background: rgba(15, 23, 42, 0.6);
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* ⚠️ 重要: グラスカード二重ネスト回避 */
/* iPadで白くなる問題を防ぐため、背景透明度を下げる */
.parent-mode-card .mode-detail-section {
  background: rgba(15, 23, 42, 0.4); /* 透明度を下げる */
}

/* サブグループコンテナ（12音階用） */
.subgroup-container {
  background: rgba(255, 255, 255, 0.03);
  border-radius: 12px;
  padding: 1rem;
  margin-bottom: 1rem;
}

.subgroup-header {
  color: #cbd5e1;
  font-size: 0.9rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
  padding-bottom: 0.5rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

/* 音程方向グリッド */
.direction-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1rem;
}

/* 音程方向カード */
.direction-card {
  background: rgba(255, 255, 255, 0.05);
  padding: 1.25rem;
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.direction-header {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 1rem;
}

.direction-title {
  color: white;
  font-size: 1rem;
  font-weight: 600;
}

.direction-stats {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0.75rem;
  margin-bottom: 1rem;
}

/* 音程別統計（ミニ表示） */
.interval-stats-mini {
  background: rgba(255, 255, 255, 0.03);
  padding: 0.75rem;
  border-radius: 8px;
}

.interval-stats-mini h6 {
  color: white;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.interval-stat-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.25rem 0;
  font-size: 0.8rem;
}

.interval-stat-row .interval-label {
  color: #94a3b8;
}

.interval-stat-row .interval-value {
  color: white;
  font-weight: 600;
}
```

### 8.3 レスポンシブ対応

```css
/* モバイル */
@media (max-width: 640px) {
  .mode-summary-stats {
    grid-template-columns: 1fr;
  }

  .direction-grid {
    grid-template-columns: 1fr;
  }

  .parent-mode-header {
    flex-wrap: wrap;
  }
}

/* タブレット */
@media (min-width: 641px) and (max-width: 1024px) {
  .mode-summary-stats {
    grid-template-columns: repeat(3, 1fr);
  }

  .direction-grid {
    grid-template-columns: 1fr;
  }
}

/* PC */
@media (min-width: 1025px) {
  .direction-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

---

## 9. ⚠️ 実装上の重要な注意点

### 9.1 インライン記述の禁止

**原則**: JavaScriptファイル内でのインラインCSS記述は避け、スタイルシートで定義する

**❌ 避けるべき実装**:
```javascript
element.style.backgroundColor = 'rgba(59, 130, 246, 0.1)';
element.style.padding = '1.5rem';
element.style.borderRadius = '16px';
```

**✅ 推奨する実装**:
```javascript
// JavaScriptではクラスの追加/削除のみ
element.classList.add('parent-mode-card');
element.setAttribute('data-level', 'beginner');
```

```css
/* CSSで定義 */
.parent-mode-card[data-level="beginner"] {
  background: rgba(59, 130, 246, 0.1);
  padding: 1.5rem;
  border-radius: 16px;
}
```

**例外**:
- 動的に計算される値（プログレスバーの幅等）のみインライン許可
  ```javascript
  progressBar.style.width = `${percentage}%`;
  ```

---

### 9.2 スタイルの重複確認

**手順**:
1. **UIカタログを確認**: `/UI-Catalog/ui-catalog-essentials.html`
2. **全CSSファイルを確認**:
   ```bash
   # 既存クラス検索（全CSSファイル対象）
   grep -r "parent-mode-card" PitchPro-SPA/styles/
   ```
3. **ページ別CSS確認**:
   - **base.css** (2,967行): 汎用共通スタイル（全ページ共通）
   - **results.css** (1,341行): 結果表示ページ
   - **training.css** (1,190行): トレーニングページ
   - **voice-range.css** (401行): 音域テストページ
   - **premium-analysis.css** (約850行): **詳細分析ページ専用** ⭐
   - **records.css** (約350行): **トレーニング記録ページ専用** ⭐
4. **重複がない場合のみ追加**

**詳細分析ページ実装時の確認順序**:
1. UIカタログ確認
2. base.css確認（共通スタイル）
3. **premium-analysis.css確認**（ページ専用スタイル）
4. 他ページのCSS確認（類似実装参考）

**既存の再利用可能なスタイル**:
- `.glass-card` - 基本的なグラスカード
- `.progress-bar` + `.progress-fill` - プログレスバー
- `.heading-md` - 見出しスタイル
- `.flex`, `.items-center`, `.gap-3` - レイアウトユーティリティ

---

### 9.3 UIカタログの活用

**確認項目**:
- ボタンスタイル → `/UI-Catalog/ui-catalog-essentials.html` のボタンセクション
- カードスタイル → Glass Cardセクション
- アイコン → Lucideアイコンセクション
- プログレスバー → プログレスバーセクション

**新規スタイル作成前のチェックリスト**:
- [ ] UIカタログに類似スタイルがないか確認
- [ ] base.cssに既存の定義がないか確認
- [ ] 他のページで同様の実装がないか確認

---

### 9.4 Lucideアイコンの統一

**初期化コンポーネント**: `window.initializeLucideIcons()`

**使用方法**:
```html
<!-- HTMLでアイコンを配置 -->
<i data-lucide="shuffle" class="text-blue-300" style="width: 24px; height: 24px;"></i>
```

```javascript
// ページ初期化時に実行
if (typeof window.initializeLucideIcons === 'function') {
  window.initializeLucideIcons({ immediate: true });
}

// 動的にDOM更新した後も実行
updateUI();
window.initializeLucideIcons({ immediate: true });
```

**バージョン固定**: v0.263.0を使用（Safari互換性のため）

**アイコン名の注意**:
- ✅ `alert-triangle`, `check-circle`, `bar-chart-2` (v0.263.0互換)
- ❌ `triangle-alert`, `circle-check-big`, `file-chart-column-increasing` (新バージョンのみ)

---

### 9.5 iPadでのグラスカード二重ネスト問題

**問題**: グラスカードを二つ重ねると、iPadで白くなる現象

**原因**:
```css
.glass-card {
  background: rgba(255, 255, 255, 0.1);
}

/* ネストすると... */
.glass-card .glass-card {
  /* 0.1 + 0.1 = 0.2 の透明度になり白くなる */
}
```

**解決策**:
```css
/* 親カード内の詳細セクションは背景透明度を下げる */
.parent-mode-card .mode-detail-section {
  background: rgba(15, 23, 42, 0.4); /* ← 暗い背景 + 低透明度 */
}

/* さらにネストする場合はもっと下げる */
.parent-mode-card .mode-detail-section .subgroup-container {
  background: rgba(255, 255, 255, 0.03); /* ← さらに低い透明度 */
}
```

**ガイドライン**:
- 1階層目: `rgba(255, 255, 255, 0.1)` または `rgba(15, 23, 42, 0.8)`
- 2階層目: `rgba(15, 23, 42, 0.4)` または `rgba(255, 255, 255, 0.05)`
- 3階層目: `rgba(255, 255, 255, 0.03)`

**テスト必須**:
- iPad実機でテスト
- Safari開発者ツールでiPadビューを確認

---

### 9.6 コード品質チェックリスト

**実装前**:
- [ ] UIカタログで既存コンポーネント確認完了
- [ ] base.cssで既存スタイル確認完了
- [ ] 他ページとの一貫性確認完了

**実装中**:
- [ ] インラインCSS使用を最小化
- [ ] クラス名の命名規則に従う（BEM風）
- [ ] Lucideアイコン初期化を忘れない

**実装後**:
- [ ] iPad実機テスト完了
- [ ] グラスカード重ね合わせの白化問題なし
- [ ] レスポンシブ対応確認（スマホ・タブレット・PC）
- [ ] 全アイコンが正しく表示される
- [ ] コンソールに警告・エラーなし

---

## 10. 実装タスク

### 10.1 Phase 1: データ構造実装（3時間）

**タスク**:
1. `MODE_DEFINITIONS`完全定義（JavaScript）
2. `normalizeSessionMode()`実装
3. `calculateParentModeStats()`実装
4. `calculateChildModeStats()`実装
5. 単体テスト（コンソールログで確認）

**成果物**:
- `premium-analysis-calculator.js` 更新

---

### 10.2 Phase 2: UI実装（4時間）

**タスク**:
1. 親モードカードHTML作成
2. 詳細展開セクションHTML作成
3. 12音階のサブグループHTML作成
4. CSS実装（`premium-analysis.css`）
5. レスポンシブ対応

**成果物**:
- `premium-analysis.html` 更新
- `premium-analysis.css` 新規作成

---

### 10.3 Phase 3: インタラクション実装（2時間）

**タスク**:
1. 折りたたみ/展開機能（JavaScript）
2. Chart.js統合（方向別比較グラフ）
3. Lucideアイコン初期化
4. アニメーション調整

**成果物**:
- `premium-analysis-controller.js` 更新

---

### 10.4 Phase 4: テスト・調整（2時間）

**タスク**:
1. 全親モードの展開/折りたたみテスト
2. iPad実機テスト（グラスカード重ね合わせ確認）
3. レスポンシブ対応確認
4. パフォーマンステスト

**成果物**:
- テスト完了レポート

---

**合計推定時間**: 11時間

---

## 11. 将来の拡張計画

### 11.1 弱点練習モードの追加（Phase 5）

**実装手順**:
1. `MODE_DEFINITIONS.parentModes`に`weakness`を追加（既に定義済み）
2. `MODE_DEFINITIONS.modes`に`weakness-ascending`, `weakness-descending`を追加（既に定義済み）
3. AI分析ロジックの実装
4. 弱点練習用のトレーニングロジック実装

**所要時間**: 8-10時間（AI分析ロジック次第）

**拡張性**: この設計では親モードの追加が容易なため、実装は1日で完了可能

---

### 11.2 さらなるモード追加

**候補**:
- 音程特化練習（3度特化、7度特化等）
- リズム統合練習
- 和音認識練習

**実装方法**:
```javascript
// 新しい親モードを追加するだけ
MODE_DEFINITIONS.parentModes['rhythm'] = {
  name: 'リズム統合練習',
  icon: 'music-4',
  color: 'cyan',
  level: '🎶',
  modes: ['rhythm-ascending', 'rhythm-descending']
};

// 個別モードを定義
MODE_DEFINITIONS.modes['rhythm-ascending'] = {
  name: 'リズム統合（上行）',
  displayName: '上行音程',
  parent: 'rhythm',
  direction: 'ascending',
  icon: 'arrow-up'
};
```

**UI**: 自動的に親モードカードが追加される（実装不要）

---

### 11.3 分析機能の拡張

**候補**:
- モード間の比較グラフ（レーダーチャート）
- 日別・週別の成長トレンド
- 音程×モードの2次元ヒートマップ
- 他ユーザーとの比較（匿名統計）

**拡張性**: Tab 4に新セクションを追加するだけで実装可能

---

## 12. 関連ドキュメント

- **元の調査報告**: `PERM-premium-analysis-comprehensive-investigation-20251119`
- **音階グループ分析設計**: `PERM-note-group-analysis-implementation-design-20251119`
- **APP_SPECIFICATION.md**: アプリケーション仕様書
- **CLAUDE.md**: 開発ガイドライン
- **UI-Catalog**: `/UI-Catalog/ui-catalog-essentials.html`

---

## 13. 承認履歴

| 日付 | バージョン | 承認者 | 備考 |
|------|-----------|--------|------|
| 2025-11-19 | 1.0.0 | ユーザー | 初版作成 |
| 2025-11-19 | 2.0.0 | ユーザー | 拡張性重視の設計に改訂、実装注意点追加 |

---

**この仕様書は、詳細分析ページの完全な設計書であり、実装時の唯一の情報源として機能します。**
