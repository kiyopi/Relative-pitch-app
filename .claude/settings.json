{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "./claude-gemini-bridge.sh hook",
            "description": "Save plan content to Gemini when exiting plan mode"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": ".*([Yy]es|はい|実行).*",
        "hooks": [
          {
            "type": "command", 
            "command": "echo 'Plan approved - saving to Gemini' && ./claude-gemini-bridge.sh save \"$(cat /dev/stdin)\"",
            "description": "Capture plan content when user says Yes"
          }
        ]
      }
    ]
  }
}